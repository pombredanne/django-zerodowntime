version: 2
jobs:
  build:
    docker:
      - image: circleci/python:2.7
    steps:
      - checkout
      - run: |
          python setup.py install
          sudo pip install tox
          python setup.py test
          tox

workflows:
  version: 2
  test:
    jobs:
      - build
